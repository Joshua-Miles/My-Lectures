<h1>Airlines</h1>
<ul>
<% @airlines.each do | airline | %>
    <li>
        <%=airline.name%>
        <%= link_to("View Details", airline) %>
    </li>
<% end %>
</ul> 

<% 

    # def link_to(name, path)
    #     puts "<a href='#{path}'>#{name}</a>"
    # end

%>